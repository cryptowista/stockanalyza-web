# Netlify config for stockanalyza.com
# Eleventy builds the site into _site. Headers are generated into _site/_headers.
# Edge function: netlify/edge-functions/markdown.js

[build]
  command = "npm run build"
  publish = "_site"

[build.environment]
  NODE_VERSION = "22"
